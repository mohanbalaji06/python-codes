public static int numTriplets(int[] arr){
        int cnt=0;
        for(int i=0;i<arr.Length;i++){
            if(((arr[i]+arr[i+1]+arr[i+2])/3)>0){
                cnt++;
            }
        }
        return cnt;
    }
