def LengthLongestPrefixSuffix(s) :
    
    
    n = len(str)
    for res in range(n // 2, 0, -1) :
        prefix = str[0: res]
        suffix = str [n - res: n]
         
        if (prefix == suffix) :
            return res
    return 0
